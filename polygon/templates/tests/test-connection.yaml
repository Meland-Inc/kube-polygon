apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polygon.fullname" . }}-test-connection"
  labels:
    {{- include "polygon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "polygon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
